#!/bin/bash -xcd 


	find . -size +5M
    find . -size +5M | grep pdf
    find . -name data*
    grep CAPTAIN linux-content/data.csv
    cd ..
    grep CAPTAIN linux-content/data.csv
    grep -r CAPTAIN
    ls -p | grep /
    ls -p | grep -v /
    env
    history
